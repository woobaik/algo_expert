const arr = [1, 2, 3, 4, 5, 6, 7, 8];

// while there is only one element left
// set up a variable iteration = 1
// if iteration 1(odd) loop through array and add (0,1) (2,3) (4)
// if iteration 2(even) loop throguth array and multiply (0,1) (2,3) (4)

// catch point => what if there is a single value, what is 1 + undefined? prolly NaN?

console.log(1 + undefined);

const arrayConversion = (arr) => {
  const result = [];
  const count = 1;
  while (result.length > 1) {
    if (count % 2 === 1) {
      // plus operation
    } else {
      // multiple operation
    }
  }
};
